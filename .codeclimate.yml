engines:
  rubocop:
    enabled: false
    #checks:
    # Rubocop/Metrics/ClassLength:
    #   enabled: false
  brakeman:
    enabled: false
  duplication:
    enabled: true
    config:
      languages:
      - ruby
ratings:
  paths:
  - src/lib/**
  - src/clients/**
  - "**.rb"
exclude_paths:
- spec/**/*
